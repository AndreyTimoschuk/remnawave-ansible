services:
  # Node Exporter для сбора системных метрик
  node_exporter:
    image: quay.io/prometheus/node-exporter:latest
    container_name: node_exporter
    command:
      - '--path.rootfs=/host'
    network_mode: host
    pid: host
    restart: unless-stopped
    volumes:
      - '/:/host:ro,rslave'

  # Prometheus для сбора и хранения метрик
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    network_mode: host
    restart: unless-stopped
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - /var/lib/monitoring/prometheus/data:/prometheus

  # Grafana для визуализации метрик
  grafana:
    image: grafana/grafana-oss:latest
    container_name: grafana
    network_mode: host
    restart: always
    environment:
      - GF_SERVER_HTTP_PORT={{ grafana_port }}
    volumes:
      - /var/lib/monitoring/grafana/data:/var/lib/grafana
    ulimits:
      nofile:
        soft: 65536
        hard: 65536

  # Xray Checker для проверки работоспособности прокси (закомментировано)
  # Раскомментируйте после развертывания панели и получения subscription URL
  # xray-checker:
  #   image: kutovoys/xray-checker
  #   container_name: xray-checker
  #   network_mode: host
  #   restart: unless-stopped
  #   user: "nonroot:nonroot"
  #   environment:
  #     - METRICS_PASSWORD=CHANGE_ME_SECURE_PASSWORD
  #     - METRICS_USERNAME=xray_checker
  #     - METRICS_HOST=0.0.0.0
  #     - METRICS_PORT=2112
  #     - METRICS_BASE_PATH=/xray/monitor
  #     - METRICS_PROTECTED=true
  #     - METRICS_INSTANCE=remnawave_check
  #     - XRAY_LOG_LEVEL=none
  #     - RUN_ONCE=false
  #     - SIMULATE_LATENCY=true
  #     - PROXY_CHECK_METHOD=status
  #     - PROXY_CHECK_INTERVAL=120
  #     - PROXY_TIMEOUT=60
  #     - SUBSCRIPTION_URL=https://panel.example.com/subscription/YOUR_SUBSCRIPTION_ID
  #     - SUBSCRIPTION_UPDATE=true
  #     - SUBSCRIPTION_UPDATE_INTERVAL=300
  #     - PROXY_IP_CHECK_URL=https://api.ipify.org?format=text
  #     - PROXY_STATUS_CHECK_URL=http://cp.cloudflare.com/generate_204
  #     - XRAY_START_PORT=10000
  #     - SSL_CERT_FILE=/etc/ssl/certs/ca-certificates.crt
  #   ulimits:
  #     nofile:
  #       soft: 65536
  #       hard: 65536

